# 당일 이슈 백로그 — 18개 (200줄 이하 단위)

> 아침에 `bash scripts/seed-issues.sh <주영> <승빈> <연성>` 한 번으로 일괄 생성.
> 순서 = 의존 순서. A(인프라)가 끝나야 B가 돌고, C·D는 B와 병렬로 간다.
> 아이디어 정의는 [IDEA.md](IDEA.md). **B가 코어고 C·D는 B에 붙는 근거층이다.**
> assign 규칙: 인프라→주영, 계산→승빈, UI/AI→연성 몫이지만 **연성은 발표 고정이므로 UI 이슈는 주영·승빈 루프에 재배분**

## A. 인프라·배포 (3) — 주영

1. **[A1] 프로젝트 뼈대 + smoke.sh 데모 시나리오**
   스택 초기화, `/` 가 뜨면 통과하도록 `scripts/smoke.sh` TODO 채움 (사람이 채우는 유일한 예외 커밋). 완료 조건: `bash scripts/smoke.sh` → SMOKE OK
2. **[A2] Vercel 연결 — main 자동 배포**
   main 머지마다 자동 배포. 완료 조건: 배포 URL 접속 시 A1 화면. **제출 링크는 별칭 도메인** (배포 URL은 로그인 벽에 막힌다)
3. **[A3] 데이터 수집·정규화**
   시간표(15042241, cp949)·화물역(15042207)·운행횟수(15068417/20) + 열차수송통계 API. 키는 `.env`, 커밋 금지. 완료 조건: `public/data/*.json` 생성

## B. 편익 계산 — 코어 (4) — 승빈

4. **[B1] OD 기준 물량·거리 산출**
   출발역·도착역·품목·물량을 받아 톤·톤키로를 계산. 실적 데이터(`ftsp_dtkm`)로 검산. 완료 조건: 샘플 3개 OD 수작업 대조 일치
5. **[B2] 환경 편익 계산 — 탄소·대기오염**
   철도 vs 도로 원단위를 곱해 배출량 차이 산출. **원단위 출처를 코드 주석에 명기**(공식 통계만). 완료 조건: 원단위와 계산식이 화면에 노출됨
6. **[B3] 사회 편익 계산 — 교통사고·도로혼잡**
   전환 물량을 트럭 대수로 환산 → 사고·혼잡 감소 추정. 완료 조건: 트럭 환산 근거가 화면에 표시됨
7. **[B4] 비교 결과 화면**
   철도 / 도로 나란히. 항목별 차이와 합계. *→ 주영·승빈 루프에 배분 가능*

## C. 현실 보정 — 시간 (3) — 승빈

8. **[C1] 시퀀스 복원 + 정차사유별 체류시간**
   열차번호 단독 키로 정렬, 자정 넘김 `% 1440`. 정차사유별 체류 집계. 완료 조건: 화물취급 5,096분 · 대피 1,733분 재현
9. **[C2] X-factor 산출**
   총 소요 ÷ 순수 주행. **분모 정의는 오전에 확정**(권장: 총소요 − 정차합). 완료 조건: 전체 평균이 약 1.3로 나옴
10. **[C3] 구간 시간 카드**
    B4 결과에 붙는 보정 카드 — "이 구간 계획 대비 실제 소요 N배, 그중 화물취급 N분". *→ 루프 배분 가능*

## D. 현실 보정 — 방향 (3) — 승빈

11. **[D1] OD 순흐름·편방향 판정**
    역별 유입−유출, 양방향/편방향 분류. 완료 조건: 편방향 249쌍(63.5%) 재현
12. **[D2] 복화 가능성 필터**
    편방향 구간의 **도착역에서 같은 품목이 나가는지** 교차 대조. 완료 조건: 77건·208,989톤(7.0%) 재현. **이 필터가 v2의 핵심 발견이다**
13. **[D3] 빈 방향 카드**
    B4 결과에 붙는 보정 카드 — "이 방향은 편방향이라 빈 화차가 돈다 / 구조적이라 채울 수 없다". 예시 주제의 '빈화차 정보 조회'에 대응. *→ 루프 배분 가능*

## E. AI층 (3) — 연성 설계, 구현은 루프 배분

14. **[E1] 자연어 입력 해석**
    "구미에서 부산까지 컨테이너 주 3회" → 출발역·도착역·품목·물량·빈도 파싱 → B1 입력. 완료 조건: 예시 문장 5개가 정확히 파싱됨
15. **[E2] 결과 서술 생성 — 화주용 / 정책용**
    같은 편익 수치를 대상별로 다르게 서술. **근거 수치 인용 필수.** 완료 조건: 두 문서가 같은 숫자를 쓰되 논지가 다름
16. **[E3] 되먹임 — 서술 속 구간 클릭 → 화면 전환**
    E2 출력의 구간 태그를 누르면 C3·D3 카드가 그 구간으로 바뀐다. *제9조 "단순 출력 나열" 방어 장면. 작게 구현해도 됨*

## F. 마무리 (2)

17. **[F1] 시연 영상 촬영·제출물 정리** — 접속 링크(별칭 도메인) + GitHub 링크 + 영상
18. **[F2] README** — 한 줄 소개, 실행법, 데이터 출처, 원단위 출처

## 생성하지 않는 것 (스코프 방어)

운송 신청·예약 / 실시간 열차 추적 / 배차 / 3사 연동 / KTDB 원본 업로드 — 발표에서 말로만.

## ⚠️ 사람이 할 일 (루프에 맡기지 않는다)

- **B2·B3의 편익 원단위 출처 확정** — 숫자를 지어내면 발표에서 무너진다. 공식 통계만
- `scripts/smoke.sh` · `.claude/commands/hack.md` 수정
